package com.company;

/**
 * EntityType contains the enums in regards to the different Entity types
 */
public enum EntityType
{
    /**
     * Enum for the standard pirate (player) type
     */
    PIRATE,

    /**
     * Enum for the standard enemy type
     */
    ENEMY,

    /**
     * Bullet enum used to access the bullet type
     */
    BULLET

}
